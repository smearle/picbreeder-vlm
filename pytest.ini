[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # neat-python 0.92 warns for every config key it defaults; our preset relies on
    # those defaults, so the noise would bury real warnings.
    ignore:Using default .*:DeprecationWarning:neat.config
