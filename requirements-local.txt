# Extra dependencies for serving LOCAL vLLM models (the `qwen3-vl-*` and
# `remote:` model paths). NOT needed for evaluations, figures, or the hosted-API
# (Gemini) path -- install these only if you want to run a local model.
#
#     uv pip install -r requirements-local.txt
#
# WHY THIS IS SEPARATE / PINNED: vLLM builds against a specific torch + CUDA. Left
# unconstrained, the resolver can silently pick an ancient source-only release
# (vllm==0.1.3, 2023) whose build imports torch and fails on any modern GPU driver.
# Pin vLLM to a recent wheel that matches YOUR installed torch + CUDA. On Blackwell
# (RTX 50-series, CUDA 12.8) install torch from the cu128 index first (see
# requirements.txt) and use a current vLLM. See:
#     https://docs.vllm.ai/en/latest/getting_started/installation.html
-r requirements.txt
vllm>=0.6.0
