__pycache__/
.DS_Store
.pycache
multirun

# secrets (community/.gitignore used to cover this; keep it covered after removal)
**/ADMIN_KEY.local.txt
.env

initial_populations/*
interactive_runs/*
# transcript bundles staged for the Hub by tools/build_all_transcripts.py (regenerable)
_transcripts_stage*/
_tx_*/
logs_collaborative*
sweep_logs*
logs/*
log/*
.env
.venv/*
.vscode/*
picture2d/rendered
outputs/*
submitit_logs/*
human_archive/*
legacy_diagrams/*
legacy_renders/*
genomeAll
**/figures/lineages
**/figures/lineages_neat
*.egg-info
archive
archive_neat
mutation_logs
cross_eval*
clip_noun_niche_es_logs/*
third-party/fer/src/archive*
third-party/fer/src/lineages*
third-party/fer/spaghetti/pbRender/thumbs_cache/
third-party/fer/spaghetti/pbRender/genomeAll/
cross_eval_clip_niches/*
SigLIP2-B-alignet*
# fer-derived renders of the human Picbreeder archive (regenerable, ~1GB)
human_lineages/*
data/human_baseline/*
# ...but DO ship the small human-baseline trajectory/metric JSONs (needed by the
# eval aggregate plots); only the large regenerable renders (res-*, PNG/PDF, the
# SigLIP2-B-alignet* model) stay ignored above/below.
!data/human_baseline/*.json
data/noun_lists/embeddings/*
_transcripts_stage/
# transcript / siglip staging scratch (large, regenerable)
_tx_stage_*/
_siglip_*.txt

# run / server logs (root-level)
/*.log
# latex build artifacts
*.aux
figures/**/*.log
# ad-hoc generated figures dropped at the repo root
/ancestry_img_*
/human_ancestry_*
/radial_*
/teaser_*
/weight_modulation_grid*
# regenerable generator outputs (dropped from the repo)
/personality_table.tex
/final_scores_table.tex
/mutation_similarities.json
# large generated artifact / data dirs (regenerable, kept out of git)
eval_models/
claude_picbreeder/
traits_meta_cluster/
colormap_tests/
# figures/: TikZ sources for the blog (+ slides/README); exclude regenerable caches
figures/metric_figs/_agg_*.npz
# the paper itself lives in Overleaf; this is its export, not a source of truth
manuscript/
